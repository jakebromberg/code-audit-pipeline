// Directory segment "__fixtures__" (universal pattern). Literal-catalog-
// inert; exercises the "__fixtures__" directory-segment rule in
// `isTestPath`.
struct FixturesDunderDirCase {
    var value: Int
}

func fixturesDunderDirCaseHelper() -> Int {
    return 1 + 1
}
