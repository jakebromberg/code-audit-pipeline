// Directory segment "__test__" (universal pattern). Literal-catalog-inert;
// exercises the "__test__" directory-segment rule in `isTestPath`.
struct DunderTestDirCase {
    var value: Int
}

func dunderTestDirCaseHelper() -> Int {
    return 1 + 1
}
