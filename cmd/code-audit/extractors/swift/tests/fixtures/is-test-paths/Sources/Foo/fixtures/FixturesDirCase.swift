// Directory segment "fixtures" (universal pattern). Literal-catalog-inert;
// exercises the "fixtures" directory-segment rule in `isTestPath`.
struct FixturesDirCase {
    var value: Int
}

func fixturesDirCaseHelper() -> Int {
    return 1 + 1
}
