{
  "cluster_id": "generic-function-candidates:app:WatchXYC:WXYC/WatchXYC/PlayerPage.swift:191:SwiftUI.Image.logo+app:iOS:WXYC/iOS/NowPlayingWidget/Extensions.swift:28:SwiftUI.Image.logo",
  "condition": "s2",
  "extraction_notes": {
    "parser_version": "1.0"
  },
  "parse_error": null,
  "parsed": {
    "category": "extract-to-common",
    "confidence": 0.82,
    "evidence_quote": "\"name\": \"SwiftUI.Image.logo\", \"kind\": \"computed-property\"",
    "rationale": "Both `app:WatchXYC` (WXYC/WatchXYC/PlayerPage.swift:191) and `app:iOS` (WXYC/iOS/NowPlayingWidget/Extensions.swift:28) define an identical computed property `SwiftUI.Image.logo` with a Jaccard similarity of 0.88, indicating near-identical implementations. Neither record carries any test, codegen, mock, or sample-app flags, so the duplication is unintentional. The property should be extracted into a shared extension file (e.g., a `SharedUI` or common extensions module that both targets already depend on, or at minimum a single file within the iOS target's shared layer) so both the Watch and iOS targets can import it from one place. If no shared module exists today, the iOS target's existing `Extensions.swift` could serve as the canonical home while the WatchXYC copy is deleted, provided the Watch target gains a dependency on that shared file or module.",
    "specifics": {
      "remove_from": [
        "WXYC/WatchXYC/PlayerPage.swift",
        "WXYC/iOS/NowPlayingWidget/Extensions.swift"
      ],
      "target_package": "app:iOS",
      "type_name": "SwiftUI.Image.logo"
    }
  },
  "query": "generic-function-candidates",
  "raw_response_path": "raw/s2/trial1/generic-function-candidates_app_WatchXYC_WXYC_WatchXYC_PlayerPage.swift_191_SwiftUI.Image.logo_app_iOS_WXYC_iOS_NowPlayingWidget_Extensions.swift_28_SwiftUI.Image.logo.json",
  "row_index": 1,
  "trial": 1
}
