{
  "cluster_id": "function-duplicates-near:Caching:Shared/Caching/Sources/Caching/CachedFetch.swift:45:cachedFetch+Caching:Shared/Caching/Sources/Caching/CachedFetch.swift:92:cachedFetch",
  "condition": "s1",
  "extraction_notes": {
    "parser_version": "1.0"
  },
  "parse_error": null,
  "parsed": {
    "category": "other",
    "confidence": 0.62,
    "evidence_quote": "\"jaccard\": 0.7",
    "rationale": "Both members are named `cachedFetch`, reside in the same file (`Shared/Caching/Sources/Caching/CachedFetch.swift`) and package (`Caching`), and carry a Jaccard body-similarity of 0.7 \u2014 indicating roughly 70 % shared implementation. Neither record is a test, mock, codegen artifact, or sample-app file, so the duplication is not intentional by policy. Because the signatures are unavailable (`fields_or_signature: []`) and both functions are co-located in one file, the most appropriate fix is an internal consolidation: extract the shared ~70 % into a private helper and have the two public overloads delegate to it, rather than any cross-package or protocol-level restructuring.",
    "specifics": {
      "proposed_action": "Inspect the two `cachedFetch` overloads at lines 45 and 92 of `Shared/Caching/Sources/Caching/CachedFetch.swift` to determine whether they share a common implementation body that can be extracted into a private helper. If the divergence is limited to a small number of call-site parameters (e.g., a cache policy, expiry duration, or key strategy), unify them into a single generic or defaulted-parameter function, removing the near-duplicate body. If the two overloads are genuinely distinct entry points that happen to share ~70 % of their logic, extract the shared logic into a private `_cachedFetch(...)` implementation function and have both public overloads delegate to it.",
      "why_no_category_fits": "Both functions live in the same file and package (`Caching` / `Shared/Caching/Sources/Caching/CachedFetch.swift`), so 'extract-to-common' (cross-package move) does not apply. The cluster provides no field/signature data (`fields_or_signature: []`), so we cannot confirm a protocol, PAT, generic type, or subclass relationship \u2014 ruling out those categories. The Jaccard similarity of 0.7 indicates meaningful but incomplete overlap, which is best addressed by an internal refactor (shared private helper or parameter consolidation) rather than any of the structural cross-cutting patterns the other categories describe."
    }
  },
  "query": "function-duplicates",
  "raw_response_path": "raw/s1/trial3/function-duplicates-near_Caching_Shared_Caching_Sources_Caching_CachedFetch.swift_45_cachedFetch_Caching_Shared_Caching_Sources_Caching_CachedFetch.swift_92_cachedFetch.json",
  "row_index": 152,
  "trial": 3
}
