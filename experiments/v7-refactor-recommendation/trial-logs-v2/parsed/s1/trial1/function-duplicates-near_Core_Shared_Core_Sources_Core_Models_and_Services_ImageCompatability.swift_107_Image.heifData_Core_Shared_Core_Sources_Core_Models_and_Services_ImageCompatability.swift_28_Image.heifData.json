{
  "cluster_id": "function-duplicates-near:Core:Shared/Core/Sources/Core/Models and Services/ImageCompatability.swift:107:Image.heifData+Core:Shared/Core/Sources/Core/Models and Services/ImageCompatability.swift:28:Image.heifData",
  "condition": "s1",
  "extraction_notes": {
    "parser_version": "1.0"
  },
  "parse_error": null,
  "parsed": {
    "category": "other",
    "confidence": 0.62,
    "evidence_quote": "\"file\": \"Shared/Core/Sources/Core/Models and Services/ImageCompatability.swift\", \"line\": 28, \"name\": \"Image.heifData\" ... \"line\": 107, \"name\": \"Image.heifData\" ... \"jaccard\": 0.75",
    "rationale": "Both participating records are `Image.heifData` methods in the same file `Shared/Core/Sources/Core/Models and Services/ImageCompatability.swift` within the `Core` package, at lines 28 and 107 respectively. The Jaccard similarity of 0.75 confirms they are near-duplicates rather than exact copies, implying a small but meaningful divergence \u2014 likely a platform-conditional split that was copy-pasted rather than properly structured. No restraint markers are set on either record (`is_test`, `is_mock`, `is_sample_app`, `is_codegen` are all false), so consolidation is warranted. The fix belongs entirely within the existing file and package rather than requiring any cross-package extraction.",
    "specifics": {
      "proposed_action": "Investigate the two `Image.heifData` method declarations at lines 28 and 107 within `Shared/Core/Sources/Core/Models and Services/ImageCompatability.swift`. With a Jaccard similarity of 0.75 they are near-duplicates but not identical, suggesting one may be a platform-conditional variant (e.g., `#if canImport(UIKit)` vs `#if canImport(AppKit)`). Consolidate into a single implementation \u2014 either by unifying the shared logic into one method body with inline conditional branches, or by extracting the common logic into a private helper and having each variant delegate to it \u2014 then delete the redundant declaration.",
      "why_no_category_fits": "Both records live in the same file (`ImageCompatability.swift`) and the same package (`Core`), so `extract-to-common` (which requires moving to a different package) does not apply. There is no protocol, subclass hierarchy, or generic type involved, so `protocol-inheritance`, `default-implementation`, `pat-introduction`, `generic-parameterization`, `subclass-lift`, and `macro-synthesis` are all inapplicable. The duplication is within a single file between two sibling method declarations, making this a straightforward intra-file deduplication that none of the named categories cleanly describe."
    }
  },
  "query": "function-duplicates",
  "raw_response_path": "raw/s1/trial1/function-duplicates-near_Core_Shared_Core_Sources_Core_Models_and_Services_ImageCompatability.swift_107_Image.heifData_Core_Shared_Core_Sources_Core_Models_and_Services_ImageCompatability.swift_28_Image.heifData.json",
  "row_index": 99,
  "trial": 1
}
