{
  "cluster_id": "generic-function-candidates:app:WatchXYC:WXYC/WatchXYC/PlayerPage.swift:191:SwiftUI.Image.logo+app:iOS:WXYC/iOS/NowPlayingWidget/Extensions.swift:28:SwiftUI.Image.logo",
  "condition": "s2",
  "extraction_notes": {
    "parser_version": "1.0"
  },
  "parse_error": null,
  "parsed": {
    "category": "extract-to-common",
    "confidence": 0.72,
    "evidence_quote": "\"name\": \"SwiftUI.Image.logo\", \"kind\": \"computed-property\"",
    "rationale": "Both `app:WatchXYC` (`WXYC/WatchXYC/PlayerPage.swift:191`) and `app:iOS` (`WXYC/iOS/NowPlayingWidget/Extensions.swift:28`) independently declare an identical `SwiftUI.Image.logo` computed property, evidenced by a Jaccard similarity of 0.8823 and matching `kind: computed-property` with empty `fields_or_signature` on both records. Neither record carries any restraint marker (`is_test`, `is_mock`, `is_sample_app`, `is_codegen` are all false), so consolidation is appropriate. The canonical definition should live in a shared extension file accessible to both targets \u2014 ideally a shared UI utilities module already depended upon by both; however, since the cluster only names `app:iOS` and `app:WatchXYC` as participating packages, the least-invasive approach is to define the extension once in a shared file within `app:iOS` (or a common extensions file) and have `app:WatchXYC` import it, removing the duplicate declaration from `WXYC/WatchXYC/PlayerPage.swift`.",
    "specifics": {
      "remove_from": [
        "WXYC/WatchXYC/PlayerPage.swift",
        "WXYC/iOS/NowPlayingWidget/Extensions.swift"
      ],
      "target_package": "app:iOS",
      "type_name": "SwiftUI.Image.logo"
    }
  },
  "query": "generic-function-candidates",
  "raw_response_path": "raw/s2/trial1/generic-function-candidates_app_WatchXYC_WXYC_WatchXYC_PlayerPage.swift_191_SwiftUI.Image.logo_app_iOS_WXYC_iOS_NowPlayingWidget_Extensions.swift_28_SwiftUI.Image.logo.json",
  "row_index": 1,
  "trial": 1
}
