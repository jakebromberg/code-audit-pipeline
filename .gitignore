node_modules/
.DS_Store
*.log

# SwiftPM build outputs and IDE metadata (extractors/swift)
.build/
.swiftpm/

# Pipeline outputs — regeneratable, not for version control
prs.json
prs-classified.json
candidates.json
catalog.json
findings.md

# Per-run scratch
/tmp/

# Audit binary (PR 3): generated query copies for `//go:embed`; canonical source is pipeline/queries/
cmd/audit/queries/
# Audit binary (PR 3): built binary
/audit
