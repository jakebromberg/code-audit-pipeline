node_modules/
.DS_Store
*.log

# SwiftPM build outputs and IDE metadata (extractors/swift)
.build/
.swiftpm/

# Pipeline outputs — regeneratable, not for version control
prs.json
prs-classified.json
candidates.json
catalog.json
findings.md

# Per-run scratch
/tmp/

# code-audit binary (PR 3): built binary
/code-audit
# strays from `go build internal/genembed/` invoked outside `go generate`
/genembed
.audit/

# Python bytecode caches. Mirrors __pycache__'s presence in
# internal/initstate.SkipDirs so the embed pruner, the `code-audit init`
# walker, and the working tree all stay in sync.
__pycache__/
*.pyc
