node_modules/
.DS_Store
*.log

# SwiftPM build outputs and IDE metadata (extractors/swift)
.build/
.swiftpm/

# Pipeline outputs — regeneratable, not for version control
prs.json
prs-classified.json
candidates.json
catalog.json
findings.md

# Per-run scratch
/tmp/

# code-audit binary (PR 3): generated query copies for `//go:embed`; canonical source is pipeline/queries/
cmd/code-audit/queries/
# code-audit binary (PR 3): built binary
/code-audit
.audit/
