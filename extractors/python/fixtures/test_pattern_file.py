"""Fixture: a file matching the `test_*.py` filename pattern (Python-specific).

Every record here should carry `is_test: true` regardless of containing dir.
"""


class TestPatternShape:
    field_alpha: str
