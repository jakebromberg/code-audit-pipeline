// Under Shared/MusicShareKit/Tests/... AND the basename ends in `Tests.swift`
// — both the directory-segment rule and the filename-suffix rule match.
struct DefaultAuthNetworkClientTests {
    var name: String
}

func defaultAuthNetworkClientTestsHelper() -> Int {
    let value = 5
    let other = 6
    let sum = value + other
    return sum
}
