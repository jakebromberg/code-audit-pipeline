// Directory segment "spec" (universal pattern). Literal-catalog-inert;
// exercises the "spec" directory-segment rule in `isTestPath`.
struct SpecDirCase {
    var value: Int
}

func specDirCaseHelper() -> Int {
    return 1 + 1
}
