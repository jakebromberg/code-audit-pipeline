// Directory segment "__tests__" (universal pattern). Literal-catalog-inert;
// exercises the "__tests__" directory-segment rule in `isTestPath`.
struct DunderTestsDirCase {
    var value: Int
}

func dunderTestsDirCaseHelper() -> Int {
    return 1 + 1
}
