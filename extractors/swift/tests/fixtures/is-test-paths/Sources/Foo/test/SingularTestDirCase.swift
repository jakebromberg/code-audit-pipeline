// Directory segment "test" (singular, universal pattern). Literal-catalog-
// inert; exercises the "test" directory-segment rule in `isTestPath`.
struct SingularTestDirCase {
    var value: Int
}

func singularTestDirCaseHelper() -> Int {
    return 1 + 1
}
