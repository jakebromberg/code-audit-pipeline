// Directory segment "e2e" (universal pattern). Literal-catalog-inert;
// exercises the "e2e" directory-segment rule in `isTestPath`.
struct E2EDirCase {
    var value: Int
}

func e2eDirCaseHelper() -> Int {
    return 1 + 1
}
