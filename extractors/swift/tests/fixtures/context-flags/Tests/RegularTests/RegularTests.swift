// Fixture for V7 §6.6 — `is_test` from path. File is under Tests/, filename
// ends with Tests.swift. Both path-segment and filename signals fire. The
// record itself is named generically and doesn't carry any mock suffix.
struct RegularBehavior {
    let value: Int
}
